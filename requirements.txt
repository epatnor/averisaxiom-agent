# === File: requirements.txt ===
fastapi
uvicorn
requests
pydantic
sqlite-utils
python-dotenv

# För framtida aktivering när vi vill köra Bluesky live
# Mastodon.py  (om vi ska lägga till Mastodon framöver)
# atproto      (Bluesky SDK)
# openai>=1.10.0 (om vi ska använda LLM-generering)

# Frontend (html/js/css) kör vi helt utanför denna backend-dependency lista
